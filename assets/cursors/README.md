# Adwaita cursors

Exported from this machine’s active theme:

- `gsettings org.gnome.desktop.interface cursor-theme` → **Adwaita**
- `gsettings org.gnome.desktop.interface cursor-size` → **24**

Source: `/usr/share/icons/Adwaita/cursors` (Xcursor → **24×24** PNG).

CSS variables: `--cursor-*` in `styles.css`. The preview uses the default arrow only.
